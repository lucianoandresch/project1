<% if (locals.errors) { %>
    <p>hay un locals.errors</p>
    <div class="errors">
        You need to fix these errors:
        <ul>
            <% errors.forEach(error => { %>
                <li>
                    <p>
                        <%= error.message %>
                    </p>
                </li>
                <% }); %>
        </ul>
    </div>
    <% } %>
        <div>
            <form id="create-form" action="<%=submitPostPath()%>" method="post">
                <div>
                    <label for="title">Title:</label>
                    <input type="text" name="title" value="Title" />
                </div>
                <div>
                    <label class="top" for="text">Text:</label>
                    <textarea class="textbox" name="text"></textarea>
                </div>
                <div>
                    <label for="image">Image:</label>
                    <input type="text" name="image" value=".png" />
                </div>
                <div>
                    <label for="creator_id">Creator:</label>
                    <input type="number" name="creator_id" />
                </div>
                <div>
                    <input class="submit" type="submit" name="create" value="CREATE NEW" />
                </div>
            </form>
        </div>